

<style>
    {# Damit die Farben aus der Theme-Configuration direkt im CSS butzbar sind, ohne sie doppelt zu definieren, werden sie hier per twig gerendert #}  
    :root {      
        

        --primary: {{primarycolor.color}};
        --primary-transparent: {{primarycolor.color}}aa;
        {% if primarycolor.color is not empty %}--primary-light: {{primarycolor.color|lighten(20)}};{% endif %}
        --primary-contrast: var(--blue-contrast);
        --primary-soft-contrast: var(--blue-soft-contrast);
        --primary-light-transparent: var(--lightblue-transparent);
        --primary-dark: var(--darkblue);

        --secondary: {{secondarycolor.color}};
        --secondary-transparent: {{secondarycolor.color}}aa;
        {% if secondarycolor.color is not empty %}--primary-light: {{secondarycolor.color|lighten(20)}};{% endif %}
        --secondary-contrast: var(--blue-contrast);
        --secondary-soft-contrast: var(--blue-soft-contrast);
        --secondary-light-transparent: var(--lightblue-transparent);
        --secondary-dark: var(--darkblue);


    }
    {% for colorset in attribute(config.theme_support, 'editor-color-palette') %}
        .has-{{colorset.slug}}-color.has-text-color {
           color: {{colorset.color}};
        }
        .text-{{colorset.slug}}-soft-contrast {
            color: var(--{{colorset.slug}}-soft-contrast);
        }
    {% endfor %}
    
   
    
</style>